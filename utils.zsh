# Change the directory to the blog dir
export BLOG_DIR='~/projects/websites/isaacbfsanders.com/_posts'
alias blog="cd $BLOG_DIR"

# Serve the current directory at localhost:3000
alias serve='python -m SimpleHTTPServer 3000'

# Open the IRC client
alias irc='irssi'
